/**
 * Public Ravel Map contract metadata.
 *
 * Structural validation remains part of the 0.1 implementation plan; keeping
 * the version and schema identifiers here gives consumers a stable import
 * while that validator is being built.
 */
export const RAVEL_MAP_VERSION = 1;
export const RAVEL_MAP_SCHEMA_ID = "https://ravel.dev/schema/ravel-map-v1.json";
